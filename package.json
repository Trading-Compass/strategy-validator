{
  "name": "strategy-validator",
  "version": "1.0.0",
  "description": "Strategies validator",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "tsc --module commonjs",
    "preversion": "npm run build",
    "postversion": "git push --follow-tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "strategy-validator"
  },
  "keywords": [
    "validator"
  ],
  "author": "Trading Compass",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "technicalindicators": "^3.1.0"
  }
}
